name: 頁面渲染

on: repository_dispatch

env:
  CACHE_SKIP_SAVE: true

jobs:
  print-number:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo 'Total workers: ${{ github.event.client_payload.total_workers }} | Worker number: ${{ github.event.client_payload.worker_number }}'
