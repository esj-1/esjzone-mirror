name: 測試

on:
  workflow_dispatch:

jobs:
  merge-branches:
    name: 合并Repo
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repo branch 📥
        uses: actions/checkout@v2
        with:
          repository: ${{ secrets.PRIVATE_REPO }}
          ref: actions-compile
          token: ${{ secrets.PA_TOKEN }}

      - name: Setup
        run: |
          git fetch --multiple origin actions-client actions-server --depth=1
          git merge origin/actions-client origin/actions-server --allow-unrelated-histories
#          for i in {0..39}; do git push origin :actions-compile-$i || true; done
